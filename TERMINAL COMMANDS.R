
##TERMINAL COMMANDS

#Connect GitHub email to RStudio
# git config --global user.email "myemail@email.com"
# git config --global user.name "My Name"

#Change cache timeout
# git config --global credential.helper 'cache --timeout 3600' (FOR ONE HOUR TIMEOUT)

#Link Repository to GitHub
# 1. Create a New Repository on GitHub with same name as local project. Ignore extra files
# 2. In terminal, change working directory to local project: pwd command yields present working directory
#     cd is command to change directory followed by new direction
# 3. git init > git add. > git commit -m "initial commit"
# 4. copy and paste code from GitGub to link repository through Terminal.